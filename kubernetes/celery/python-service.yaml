apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: default
spec:
  selector:
    app: celery-worker
  ports:
    - name: api
      port: 8082
      targetPort: 8082
  type: ClusterIP
